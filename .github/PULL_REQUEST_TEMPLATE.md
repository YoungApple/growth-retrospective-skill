## What

One or two sentences. What changes?

## Why

What failure mode does this prevent / which user pain does this address? Reference an issue if there is one.

## Tested in

- [ ] Claude Code
- [ ] OpenAI Codex CLI
- [ ] Google Antigravity
- [ ] N/A (docs / examples only)

## Checklist

- [ ] If touching `SKILL.md`, line count stayed roughly the same or shrunk
- [ ] If touching `references/`, the new content is grep-able and self-contained
- [ ] If adding a script, it works on a fresh clone with only `git` installed
- [ ] Frontmatter `name` + `description` in `SKILL.md` still match repo name
- [ ] AI assistance used? (not disqualifying, but say so — changes review focus)

## Out of scope

What this PR explicitly does NOT change.
